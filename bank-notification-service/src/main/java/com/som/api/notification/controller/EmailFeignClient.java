package com.som.api.notification.controller;



/*@FeignClient(name = "bank-notification-service") // service name in Eureka
*/public interface EmailFeignClient {
	/*
	 * @PostMapping("/api/notification/email/send") EmailResponse
	 * sendEmail(@RequestBody EmailRequest request);
	 */
}
